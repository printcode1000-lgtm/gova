/**
 * Versions shipped by the current repository state.
 *
 * The native shell and web content advance independently. Environment values
 * may pin a release build, while these constants keep ordinary web/dev builds
 * and local Capacitor assets aligned with the versions already committed in
 * Android, iOS, and the OTA manifest.
 */
export const CURRENT_NATIVE_APP_VERSION = "0.2.3";
export const CURRENT_WEB_CONTENT_VERSION = "0.1.15";
