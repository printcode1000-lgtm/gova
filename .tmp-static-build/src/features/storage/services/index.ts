/**
 * Client entry point for image storage.
 * UI and hooks import from here — never the API adapter directly.
 */
export { imageStorageService } from './image-storage-service';
export type { IImageStorageService } from './image-storage-service.interface';
