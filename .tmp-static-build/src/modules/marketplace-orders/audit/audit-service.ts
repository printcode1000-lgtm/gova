import { randomUUID } from "node:crypto"; import type { MarketplaceDb } from "../db/client"; import type { AuditInput } from "../domain/types";
export async function writeAuditLog(db:MarketplaceDb,input:AuditInput){const now=new Date().toISOString(); await db.execute("INSERT INTO audit_trail (id,order_id,entity_type,entity_id,action,old_status,new_status,old_value_json,new_value_json,performed_by,performed_by_role,reason,source,ip_address,notes,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)",[randomUUID(),input.orderId,input.entityType,input.entityId,input.action,input.oldStatus??null,input.newStatus??null,input.oldValue===undefined?null:JSON.stringify(input.oldValue),input.newValue===undefined?null:JSON.stringify(input.newValue),input.actor.id,input.actor.role,input.reason??null,input.actor.source??null,input.actor.ipAddress??null,input.notes??null,now,now]);}

