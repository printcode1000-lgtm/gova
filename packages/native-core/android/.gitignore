# Gradle outputs for the Capacitor :native-core Android library module.
# Long paths under build/.transforms/ break `git add -A` on Windows during deploy.
# /build/ is anchored to this module so previously tracked intermediates stay ignored.
.gradle/
/build/
build/
