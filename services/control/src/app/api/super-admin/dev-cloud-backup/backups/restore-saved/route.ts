import { devCloudBackupService } from '@/control/dev-cloud-backup'; import { runControlSuperAdminJsonRoute } from '@/control/super-admin-route';
export async function POST(request: Request) { return runControlSuperAdminJsonRoute<{ fileName?: unknown; mode?: unknown; confirmationText?: unknown }, unknown>(request, ({ body }) => { const fileName = typeof body.fileName === 'string' ? body.fileName : ''; if (!fileName) throw new Error('devCloudBackupFileRequired'); const mode = body.mode === 'replace' ? 'replace' : 'merge'; return devCloudBackupService.restoreSavedBackup(fileName, mode, typeof body.confirmationText === 'string' ? body.confirmationText : ''); }); }
