import { googlePlayStoreAssetsService } from '@/control/google-play'; import { runControlSuperAdminRoute } from '@/control/super-admin-route';
export async function POST(request: Request) { return runControlSuperAdminRoute(request, async () => { const form = await request.formData(); const file = form.get('file'); if (!(file instanceof File)) throw new Error('googlePlayMappingFileRequired'); return googlePlayStoreAssetsService.uploadMapping({ versionCode: String(form.get('versionCode') || ''), fileName: file.name, contentType: file.type || 'text/plain', buffer: Buffer.from(await file.arrayBuffer()) }); }); }
