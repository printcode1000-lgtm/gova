import 'server-only';

/** Control owns Super Admin user administration; exact service seam only. */
export { superAdminUserService } from '@/features/super-admin/server/services/super-admin-user-service.server';
