{
  "version": 1,
  "requestId": "asolcutover-fix-config-layer-test-env-20260831-190400",
  "agentId": "chatgpt-sol",
  "workflow": "local-agent-main",
  "mode": "main",
  "ref": "main",
  "inputs": {
    "agent_id": "chatgpt-sol",
    "commit_message": "Move synthetic runtime test environment into configuration layer",
    "verification": "docs-ci",
    "timeout_minutes": "10",
    "execution_target": "isolated-worktree",
    "shell_command": "set -euo pipefail\ncat > src/core/config/runtime-test-environment.ts <<'EOF'\nconst SYNTHETIC_RUNTIME_KEYS = [\n  \"NODE_ENV\",\n  \"ASOL_MODE\",\n  \"NEXT_PUBLIC_ASOL_MODE\",\n  \"VERCEL\",\n  \"VERCEL_ENV\",\n  \"ASOL_DATA_SOURCE\",\n  \"ASOL_PROVISIONING\",\n  \"GITHUB_ACTIONS\",\n] as const;\n\nexport async function withSyntheticDevelopmentRuntime<T>(\n  run: () => Promise<T>,\n): Promise<T> {\n  const original = Object.fromEntries(\n    SYNTHETIC_RUNTIME_KEYS.map((key) => [key, process.env[key]]),\n  );\n  try {\n    for (const key of SYNTHETIC_RUNTIME_KEYS) delete process.env[key];\n    Object.assign(process.env, {\n      NODE_ENV: \"development\",\n      ASOL_MODE: \"development\",\n      ASOL_DATA_SOURCE: \"local\",\n    });\n    return await run();\n  } finally {\n    for (const key of SYNTHETIC_RUNTIME_KEYS) {\n      const value = original[key];\n      if (value === undefined) delete process.env[key];\n      else process.env[key] = value;\n    }\n  }\n}\nEOF\npython3 - <<'PY'\nfrom pathlib import Path\np = Path('src/features/release-commands/tests/release-commands.test.ts')\ns = p.read_text()\nanchor = 'import path from \"node:path\";\\n'\nimp = 'import path from \"node:path\";\\n\\nimport { withSyntheticDevelopmentRuntime } from \"@/core/config/runtime-test-environment\";\\n'\nif 'runtime-test-environment' not in s:\n    if anchor not in s: raise SystemExit('import anchor not found')\n    s = s.replace(anchor, imp, 1)\nstart = s.find('const SYNTHETIC_RUNTIME_KEYS = [')\nend = s.find('async function main() {')\nif start >= 0:\n    if end < start: raise SystemExit('helper end not found')\n    s = s[:start] + s[end:]\np.write_text(s)\n\ndoc = Path('docs/01-architecture/10-application-layers/configuration-layer.md')\nd = doc.read_text()\nrow = '| `runtime-test-environment.ts` | Test-only synthetic runtime fixture; snapshots/restores runtime environment inputs inside the Configuration layer |\\n'\nif row not in d:\n    marker = '| `runtime-context.server.ts` | Server and build runtime resolver (`getServerRuntimeContext()`) |\\n'\n    if marker not in d: raise SystemExit('configuration doc anchor not found')\n    d = d.replace(marker, marker + row, 1)\n    doc.write_text(d)\n\nlog = Path('note/asol-control-local-runner-operations-log.md')\ntext = log.read_text()\nincident = '''\\n## Incident 12 — test fixture violated the Configuration-layer boundary\\n\\n**Symptom:** the second production cutover passed production doctor, Vercel account access, typecheck, lint, and knowledge generation, then `architecture:check` failed at 95% because `src/features/release-commands/tests/release-commands.test.ts` read `process.env` directly.\\n\\n**Root cause:** the runtime-isolation fix was behaviorally correct but placed environment access in a feature test. The architecture contract permits `process.env` only under `src/core/config/`.\\n\\n**Recovery:** move the synthetic runtime snapshot/set/restore helper into the Configuration layer and import it from the release-command test. Re-run the hostile-CI release test, architecture check, global typecheck/lint, docs generation, and docs CI.\\n\\n**Do not repeat:** test code is not exempt from architectural boundaries; environment fixtures must enter through the Configuration layer just like production code.\\n'''
if '## Incident 12 — test fixture violated the Configuration-layer boundary' not in text:\n    log.write_text(text.rstrip() + '\\n' + incident + '\\n')\nPY\n\necho '=== hostile release test through config fixture ==='\nenv GITHUB_ACTIONS=true VERCEL_ENV=preview ASOL_DATA_SOURCE=cloud ASOL_PROVISIONING=true npm run test:release-commands\necho '=== architecture ==='\nnpm run architecture:check\necho '=== global parser/type gates ==='\nnpm run typecheck\nnpm run lint\necho '=== regenerate knowledge ==='\nnpm run docs:generate"
  },
  "createdAt": "2026-08-31T19:03:30.000Z"
}