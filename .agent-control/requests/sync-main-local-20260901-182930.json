{
  "version": 1,
  "requestId": "sync-main-local-20260901-182930",
  "agentId": "chatgpt",
  "workflow": "local-agent-main",
  "mode": "main",
  "ref": "main",
  "inputs": {
    "agent_id": "chatgpt",
    "commit_message": "chore: sync canonical local checkout to latest main",
    "shell_command": "set -euo pipefail; repo=/home/hesham/gova; backup=/home/hesham/.local/share/gova-backups/main-sync-20260901-212930; mkdir -p \"$backup\"; test \"$(git -C \"$repo\" branch --show-current)\" = main; git -C \"$repo\" fetch --prune origin main; before=$(git -C \"$repo\" rev-parse HEAD); remote=$(git -C \"$repo\" rev-parse origin/main); printf '%s\\n' \"$before\" > \"$backup/local-head-before.txt\"; printf '%s\\n' \"$remote\" > \"$backup/origin-main-before.txt\"; git -C \"$repo\" status --short --branch > \"$backup/status-before.txt\"; git -C \"$repo\" log --oneline --decorate --graph --max-count=30 --all > \"$backup/log-before.txt\"; if [ -n \"$(git -C \"$repo\" status --porcelain)\" ]; then git -C \"$repo\" stash push -u -m 'chatgpt pre-sync 2026-09-01 21:29 Cairo'; fi; if git -C \"$repo\" rev-parse -q --verify refs/stash >/dev/null; then git -C \"$repo\" rev-parse refs/stash > \"$backup/stash-ref.txt\"; git -C \"$repo\" bundle create \"$backup/local-before.bundle\" HEAD refs/stash; else git -C \"$repo\" bundle create \"$backup/local-before.bundle\" HEAD; fi; git -C \"$repo\" reset --hard origin/main; test -z \"$(git -C \"$repo\" status --porcelain)\"; test \"$(git -C \"$repo\" rev-parse HEAD)\" = \"$(git -C \"$repo\" rev-parse origin/main)\"; git -C \"$repo\" rev-parse HEAD > \"$backup/local-head-after.txt\"; git -C \"$repo\" status --short --branch > \"$backup/status-after.txt\"; echo \"SYNCED_HEAD=$(git -C \"$repo\" rev-parse HEAD)\"; echo \"BACKUP=$backup\"",
    "verification": "none",
    "execution_target": "isolated-worktree",
    "timeout_minutes": "10"
  },
  "createdAt": "2026-09-01T18:29:30.000Z"
}
