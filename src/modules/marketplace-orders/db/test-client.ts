import fs from "node:fs"; import path from "node:path"; import Database from "better-sqlite3"; import type { MarketplaceDb } from "./client";
export function createMemoryMarketplaceDb():MarketplaceDb { const sqlite=new Database(":memory:");sqlite.pragma("foreign_keys = ON");sqlite.exec(fs.readFileSync(path.join(process.cwd(),"src/modules/marketplace-orders/db/migrations/0000_marketplace_orders.sql"),"utf8"));const db:MarketplaceDb={async execute(sql,args=[]){const values=args.map(v=>typeof v==="boolean"?(v?1:0):v),s=sqlite.prepare(sql);return /^\s*(SELECT|WITH|PRAGMA)/i.test(sql)?s.all(...values) as any:[{changes:s.run(...values).changes}]},async transaction(work){sqlite.exec("BEGIN");try{const result=await work(db);sqlite.exec("COMMIT");return result}catch(error){sqlite.exec("ROLLBACK");throw error}}};return db }
