Rebuild the entire architecture documentation under:

```text
docs/01-architecture/
```

using the preserved backup:

```text
docs/01-architecture-backup/
```

as the historical knowledge source, while treating the **current repository implementation as the authoritative source for the current architecture**.

This is not a file-moving or documentation-cleanup task.

It is a complete reconstruction of `docs/01-architecture/` into a **canonical, repository-grounded, Agent-First Architecture Knowledge Base** designed primarily for coding agents such as Claude Code, Codex, Cursor, and similar autonomous development agents.

The final documentation must be:

```text
Precise
Explicit
Canonical
Searchable
Traceable
Relational
Code-grounded
Agent-readable
Machine-retrievable
Non-ambiguous
Non-duplicative
Current
Comprehensive
```

The fundamental objective is:

> A future coding agent should be able to understand the repository architecture, capability ownership, package boundaries, allowed and forbidden dependencies, composition model, runtime boundaries, mandatory gateways, infrastructure ownership, enforcement mechanisms, and safe extension rules directly from `docs/01-architecture/` without having to infer architectural intent from scattered code.

---

# 1. Preserve the backup completely

The directory:

```text
docs/01-architecture-backup/
```

MUST NOT be modified, deleted, renamed, reorganized, or rewritten.

Treat it as a preserved historical source.

Read **every file and every subdirectory inside it** before declaring the migration complete.

Do not skip files because they appear outdated, repetitive, historical, or poorly organized.

Every meaningful architectural statement from the backup must be classified and accounted for.

---

# 2. Remove the current architecture documentation first

After understanding the backup and before reconstructing the documentation:

Delete all files and subdirectories currently inside:

```text
docs/01-architecture/
```

Then recreate the directory from scratch.

Do not delete:

```text
docs/01-architecture/
```

itself if unnecessary.

Do not touch:

```text
docs/01-architecture-backup/
```

---

# 3. Do not trust the backup blindly

The backup is a knowledge source, not the final source of truth.

For every meaningful architectural claim found in the backup, verify it against the current repository.

The current code determines the **current implementation**, but architectural intent from the backup may still remain valid even when implementation details changed.

Before deciding that an old statement is obsolete, perform deep verification.

At minimum inspect where relevant:

* package source code,
* `package.json`,
* package `exports`,
* TypeScript path mappings,
* actual imports,
* re-exports,
* composition roots,
* ports,
* contracts,
* adapters,
* infrastructure implementations,
* runtime-specific entry points,
* application consumers,
* services,
* scripts,
* architecture enforcement,
* architecture tests,
* repository configuration,
* related documentation outside `01-architecture`.

Do not discard a statement merely because:

* a file was renamed,
* a function was renamed,
* a package was reorganized,
* an implementation moved,
* a wrapper changed,
* a relative path changed,
* the old wording no longer matches the exact implementation.

Always distinguish between:

```text
Architectural invariant
```

and:

```text
Current implementation mechanism
```

If the mechanism changed but the invariant remains valid, preserve the invariant and document the new implementation.

---

# 4. Zero-loss knowledge migration

The migration MUST NOT silently lose valid architectural knowledge.

For every meaningful statement from the backup classify it internally as one of:

```text
PRESERVED
→ rewritten in a new canonical document

MERGED
→ consolidated with equivalent information

SPLIT
→ distributed across multiple focused documents

MOVED
→ relocated to another documentation domain

HISTORICAL
→ represented as an ADR or historical decision

SUPERSEDED
→ replaced by verified current architecture

OMITTED
→ proven incompatible with current repository behavior
```

No backup file may remain unreviewed.

No meaningful architectural knowledge may disappear merely because the old taxonomy is being replaced.

---

# 5. Required baseline structure

Create at least the following structure:

```text
docs/
└── 01-architecture/
    │
    ├── README.md
    │
    ├── 01-principles/
    │   ├── architecture-principles.md
    │   ├── capability-ownership.md
    │   ├── single-responsibility.md
    │   └── dependency-direction.md
    │
    ├── 02-packages/
    │   ├── package-model.md
    │   ├── package-layers.md
    │   ├── package-naming.md
    │   ├── package-exports.md
    │   ├── package-creation-rules.md
    │   └── module-isolation-rules.md
    │
    ├── 03-dependencies/
    │   ├── dependency-rules.md
    │   ├── allowed-dependencies.md
    │   ├── forbidden-dependencies.md
    │   ├── ports-and-contracts.md
    │   └── dependency-inversion.md
    │
    ├── 04-composition/
    │   ├── composition-model.md
    │   ├── composition-roots.md
    │   ├── dependency-wiring.md
    │   └── service-composition.md
    │
    ├── 05-capability-enforcement/
    │   ├── mandatory-gateways.md
    │   ├── capability-closure.md
    │   ├── infrastructure-ownership.md
    │   ├── default-deny-model.md
    │   └── bypass-prevention.md
    │
    ├── 06-runtime-boundaries/
    │   ├── browser-server-boundaries.md
    │   ├── runtime-isolation.md
    │   ├── service-boundaries.md
    │   └── application-package-boundaries.md
    │
    ├── 07-enforcement/
    │   ├── architecture-check.md
    │   ├── package-sealing.md
    │   ├── import-enforcement.md
    │   ├── architecture-tests.md
    │   └── enforcement-exceptions.md
    │
    ├── 08-reference/
    │   ├── capability-map.md
    │   ├── package-catalog.md
    │   ├── dependency-map.md
    │   └── architecture-diagrams.md
    │
    └── 09-decisions/
        ├── README.md
        └── ADR-xxxx-*.md
```

This structure is a required baseline, not a hard maximum.

You MAY create additional directories or documents if the repository contains a real architectural responsibility that does not fit this taxonomy.

Any additional directory MUST represent a coherent architectural knowledge category.

Any additional document MUST follow Single Responsibility.

Do not create directories merely to make files shorter.

---

# 6. Agent-First Documentation is mandatory

Every document under:

```text
docs/01-architecture/
```

MUST follow an **Agent-First Documentation Standard**.

The documentation is primarily intended to be:

* searched by agents,
* semantically retrieved,
* parsed in chunks,
* reasoned over,
* followed during implementation,
* used to determine safe modification boundaries,
* used to locate the correct owner of functionality.

Optimize for machine comprehension and architectural decision-making, not visual presentation.

---

# 7. Text is always the source of truth

Never place unique architectural information only inside:

* images,
* screenshots,
* visual diagrams,
* exported graphics,
* Mermaid diagrams,
* flowchart images,
* tables that are impossible to interpret without visual context.

All architectural facts MUST exist in plain searchable text.

A visual representation MAY be included as secondary material, but:

> Every fact represented visually MUST also be stated explicitly in text.

Images MUST NOT be required to understand the architecture.

---

# 8. Prefer explicit textual architecture graphs

Prefer:

```text
UI
→ Composition
→ Capability Owner
→ Port
→ Authorized Implementation
→ Infrastructure
→ Final Side Effect
```

over purely graphical diagrams.

When documenting an actual repository flow, replace generic terms with real identifiers.

Example:

```text
ProductPage
→ useProductMutation
→ product composition
→ @asol/product-core
→ ProductPersistencePort
→ product persistence adapter
→ @asol/data-core/server
→ Turso
```

Then explain the responsibility of each step.

---

# 9. Always use exact repository identifiers

Avoid ambiguous language such as:

```text
the package
the service
the database layer
this module
that helper
the implementation
the adapter above
```

when an exact identifier exists.

Prefer exact names such as:

```text
@asol/product-core
@asol/data-core/server
ProductPersistencePort
src/features/product-core-ports.ts
packages/native-core/package.json
npm run architecture:check
```

Use exact:

* package names,
* package export paths,
* file paths,
* function names,
* interface names,
* port names,
* adapter names,
* scripts,
* runtime entry points,
* configuration files.

This is mandatory because agents must be able to search the repository directly from documentation.

---

# 10. Use stable relationship vocabulary

Document architectural relationships using explicit standardized terms whenever applicable.

Use terms such as:

```text
OWNS
REQUIRES
PROVIDES
IMPLEMENTED_BY
COMPOSED_BY
CONSUMED_BY
ALLOWED_TO_IMPORT
MUST_NOT_IMPORT
MUST_PASS_THROUGH
SOURCE_OF_TRUTH
FINAL_SIDE_EFFECT
RUNS_IN
DELEGATES_TO
VALIDATED_BY
AUTHORIZED_BY
PERSISTS_THROUGH
```

Example:

```text
@asol/product-core
OWNS → authoritative product mutation rules

@asol/product-core
REQUIRES → ProductPersistencePort

ProductPersistencePort
IMPLEMENTED_BY → product persistence adapter

Product mutations
MUST_PASS_THROUGH → @asol/product-core
```

The goal is to make the documentation behave like a textual architectural knowledge graph.

---

# 11. Use normative terminology consistently

Architectural requirements MUST use:

```text
MUST
MUST NOT
SHOULD
MAY
```

Do not express mandatory architecture rules using weak wording such as:

```text
prefer
normally
usually
ideally
when possible
typically
```

unless the behavior is genuinely optional.

---

# 12. Every important document must define Purpose and Scope

Each document MUST clearly identify:

```text
## Purpose
```

and:

```text
## Scope
```

The reader must immediately know:

* why the document exists,
* what architecture it governs,
* what it does not govern.

---

# 13. Document responsibility and non-responsibility

Every capability/package-oriented document MUST explicitly contain:

```text
## Responsibilities
```

and:

```text
## Non-Responsibilities
```

Agents must understand both:

* what a capability owns,
* what it MUST NOT own.

This is critical for preventing capability creep.

---

# 14. Document canonical ownership

Every significant capability MUST have exactly one documented authoritative owner.

The canonical ownership source SHOULD be:

```text
docs/01-architecture/08-reference/capability-map.md
```

Other documents MUST reference that source rather than independently maintaining conflicting ownership tables.

Apply:

> One architectural fact → one canonical source of truth.

---

# 15. Avoid duplicate sources of truth

Do not copy the same authoritative information into many documents.

Examples that should have canonical ownership:

* capability ownership,
* package classification,
* dependency permissions,
* runtime ownership,
* infrastructure ownership,
* package exports,
* composition roots.

Other documents may summarize or reference those facts, but MUST link to their canonical document.

---

# 16. capability-map.md must be a primary machine-readable reference

Create:

```text
docs/01-architecture/08-reference/capability-map.md
```

as one of the most important architecture documents.

For each significant capability include a consistent record such as:

```text
Capability:
Owner Package:
Architectural Layer:
Public Gateway:
Allowed Consumers:
Required Ports:
Composition Root:
Infrastructure Owner:
Final Side Effect:
Authoritative Validation:
Authoritative Authorization:
Forbidden Bypasses:
Canonical Documents:
Status:
```

Where applicable, use:

```text
Status: CLOSED
```

only when the capability satisfies the documented closure requirements.

The map MUST reflect the actual current repository.

Do not populate it from package names alone.

Discover capabilities from code behavior.

---

# 17. package-catalog.md must describe every relevant package

Create:

```text
docs/01-architecture/08-reference/package-catalog.md
```

For each package include at least:

```text
Package:
Purpose:
Architectural Layer:
Owned Capabilities:
Public Exports:
Runtime:
Allowed Dependencies:
Forbidden Dependencies:
Required Ports:
Composition:
Infrastructure Privileges:
Canonical Documentation:
```

Verify these facts directly from the repository.

Do not document imaginary or historical exports.

---

# 18. dependency-map.md must describe actual dependency relationships

Create:

```text
docs/01-architecture/08-reference/dependency-map.md
```

Use explicit relationship notation.

Example:

```text
@asol/orders-core
ALLOWED_TO_IMPORT → @asol/product-core/contracts

@asol/orders-core
MUST_NOT_IMPORT → @asol/product-core/internal

@asol/orders-core
REQUIRES → ProductLookupPort

ProductLookupPort
IMPLEMENTED_BY → ...
```

Distinguish:

* package dependency,
* contract dependency,
* composition dependency,
* runtime dependency,
* infrastructure dependency.

Do not rely only on a visual graph.

---

# 19. architecture-diagrams.md is secondary

Create:

```text
docs/01-architecture/08-reference/architecture-diagrams.md
```

Use primarily ASCII/text diagrams.

Example:

```text
Application Entry Point
        ↓
Composition
        ↓
Capability Owner
        ↓
Minimal Port
        ↓
Authorized Infrastructure Adapter
        ↓
External Side Effect
```

Mermaid MAY be used if useful.

However:

> `architecture-diagrams.md` MUST NOT contain any architectural fact that is unavailable elsewhere as explicit text.

---

# 20. Use a standard document schema where applicable

Capability, package, enforcement, runtime, and composition documents SHOULD follow this structure when applicable:

```text
# Title

## Purpose
## Scope
## Source of Truth
## Ownership
## Responsibilities
## Non-Responsibilities
## Public Entry Points
## Allowed Consumers
## Dependencies
## Forbidden Dependencies
## Ports and Contracts
## Composition
## Execution Flow
## Data Flow
## Runtime Boundaries
## Invariants
## Forbidden Bypasses
## Failure Behavior
## Source Map
## Related Documents
## Change Impact
```

Not every section must exist when irrelevant, but omission must be intentional.

---

# 21. Execution Flow must be explicit and end-to-end

Important capabilities MUST document actual end-to-end execution paths.

Do not stop at wrapper functions.

Trace to the final side effect.

Example:

```text
src/app/...
→ src/features/...
→ @asol/product-core
→ ProductPersistencePort
→ authorized adapter
→ @asol/data-core/server
→ Turso write
```

Then explain each stage.

For each step identify:

```text
Identifier:
Responsibility:
Input:
Output:
Delegates To:
```

when useful.

---

# 22. Document final side effects explicitly

For important flows, identify the real final side effect.

Examples:

```text
FINAL_SIDE_EFFECT → Turso mutation
FINAL_SIDE_EFFECT → SQLite write
FINAL_SIDE_EFFECT → R2 object upload
FINAL_SIDE_EFFECT → notification provider request
FINAL_SIDE_EFFECT → Capacitor native API
FINAL_SIDE_EFFECT → filesystem write
FINAL_SIDE_EFFECT → external HTTP service
```

Do not end flow documentation with vague statements such as:

```text
the service saves the data
```

---

# 23. Document data flow separately when needed

For data-centric capabilities use explicit flow:

```text
Input
→ Transport Validation
→ Domain Validation
→ Normalization
→ Domain Transformation
→ Persistence Mapping
→ Storage
→ Read Mapping
→ Output
```

Identify which package owns each stage.

---

# 24. Document invariants separately from implementation

Every important architecture document SHOULD include:

```text
## Invariants
```

An invariant describes what MUST remain true even if implementation details change.

Example:

```text
Invariant:
Every authoritative page mutation MUST pass through the page-save capability owner.
```

Then separately document:

```text
Current Implementation:
...
```

This prevents documentation from freezing implementation accidents.

---

# 25. Never pin mechanisms unnecessarily

Avoid rules such as:

```text
This exact file path MUST always exist.
```

unless the exact path is itself an intentional architecture contract.

Prefer:

```text
Invariant:
There MUST be exactly one authorized composition boundary for this capability.

Current implementation:
`src/features/...`
```

This distinction is mandatory.

---

# 26. Document forbidden bypasses explicitly

Every major capability MUST have:

```text
## Forbidden Bypasses
```

Examples:

```text
MUST NOT:
Page → raw database

MUST NOT:
Route → repository implementation

MUST NOT:
Hook → R2 SDK

MUST NOT:
Application code → package internals

MUST NOT:
Capability → unrelated vendor SDK

MUST NOT:
New package → privileged infrastructure without explicit ownership
```

Agents must be told not only the intended path, but also the architectural shortcuts that are invalid.

---

# 27. Explain important rules with reason and failure prevented

For important rules use:

```text
Rule:
Reason:
Failure prevented:
```

Example:

```text
Rule:
Application code MUST NOT import the raw database client.

Reason:
Database infrastructure ownership belongs to the authorized data boundary.

Failure prevented:
Creation of an alternative persistence path that bypasses domain validation and mandatory capability gateways.
```

This is important because agents otherwise tend to “simplify” constraints they do not understand.

---

# 28. Document package creation rules comprehensively

Create:

```text
docs/01-architecture/02-packages/package-creation-rules.md
```

It MUST clearly explain:

* when a new package MUST be created,
* when a new package SHOULD be created,
* when a new package MUST NOT be created,
* capability ownership criteria,
* SRP criteria,
* architectural layer selection,
* naming rules,
* public exports,
* runtime boundaries,
* ports/contracts,
* composition requirements,
* infrastructure permissions,
* dependency restrictions,
* sealing requirements,
* architecture enforcement implications.

A future agent should be able to create a correct new package by following this document.

---

# 29. Document package layers explicitly

Create:

```text
docs/01-architecture/02-packages/package-layers.md
```

Describe the actual package layering model used by the repository.

For every layer document:

```text
Purpose:
Allowed dependencies:
Forbidden dependencies:
Runtime expectations:
Typical responsibilities:
Examples:
```

Use actual repository packages as examples.

---

# 30. Document package sealing exactly as implemented

Create:

```text
docs/01-architecture/07-enforcement/package-sealing.md
```

Verify and document the real mechanisms enforcing sealed packages.

Inspect:

* `package.json` exports,
* import restrictions,
* relative path restrictions,
* deep import restrictions,
* architecture checker behavior,
* test enforcement,
* alias behavior,
* new package handling.

Do not document a theoretical package seal.

Document the actual current seal.

---

# 31. Document architecture enforcement from implementation, not memory

For:

```text
docs/01-architecture/07-enforcement/
```

inspect the current enforcement implementation directly.

Document exactly how the repository handles:

* static imports,
* dynamic imports,
* `require`,
* re-exports,
* relative imports,
* package deep imports,
* path aliases,
* vendor SDK ownership,
* raw database access,
* storage SDK ownership,
* package internals,
* root-level files,
* unknown directories,
* newly created packages,
* allowlists,
* exclusions.

Do not claim a rule exists unless the repository actually enforces it.

---

# 32. Document default-deny behavior

Create:

```text
docs/01-architecture/05-capability-enforcement/default-deny-model.md
```

Explain precisely:

* which architectural privileges are denied by default,
* which locations receive no implicit infrastructure authority,
* how new packages are treated,
* how new directories are treated,
* how privileged SDK access is granted,
* how exceptions are represented,
* what is still location-sensitive if anything.

Verify all statements against the current enforcement implementation.

---

# 33. Document mandatory gateway semantics

Create:

```text
docs/01-architecture/05-capability-enforcement/mandatory-gateways.md
```

Define exactly what “mandatory” means.

A gateway is not mandatory merely because current code uses it.

Document the invariant:

```text
Caller
→ Mandatory Gateway
→ Authorized Implementation
→ Final Side Effect
```

and explicitly prohibit:

```text
Caller
→ Raw Infrastructure
→ Final Side Effect
```

or:

```text
Caller
→ Alternative Helper
→ Final Side Effect
```

---

# 34. Document capability closure

Create:

```text
docs/01-architecture/05-capability-enforcement/capability-closure.md
```

Define a capability as `CLOSED` only when all applicable conditions hold:

```text
Exactly one authoritative owner
+
Controlled public gateway
+
All legitimate paths pass through the owner
+
No independent semantic implementation
+
No unauthorized raw infrastructure path
+
Internal privileged primitives are inaccessible
+
Contracts expose least authority
+
Composition is controlled
+
Static enforcement protects the boundary
```

Document how an agent should verify closure when modifying or adding a capability.

---

# 35. Document infrastructure ownership explicitly

Create:

```text
docs/01-architecture/05-capability-enforcement/infrastructure-ownership.md
```

Enumerate privileged infrastructure classes used by the project.

For each relevant primitive document:

```text
Infrastructure:
Direct Owner:
Authorized Direct Consumers:
Public Abstraction:
Forbidden Direct Consumers:
Runtime:
Reason:
```

Include actual infrastructure such as, where present:

* Turso,
* SQLite,
* Drizzle,
* R2,
* S3-compatible APIs,
* filesystem,
* native APIs,
* notification SDKs,
* authentication providers,
* external platform SDKs.

Do not infer ownership solely from dependency declarations.

Inspect actual usage.

---

# 36. Ports and contracts must be documented as authority boundaries

Create:

```text
docs/01-architecture/03-dependencies/ports-and-contracts.md
```

Explain that ports/contracts do not merely decouple code; they also constrain authority.

Document:

* where contracts should live,
* who owns them,
* who implements them,
* how composition binds them,
* why consumers should receive minimal interfaces,
* why generic raw clients are forbidden when a narrow port is sufficient.

Use real examples from the repository.

---

# 37. Document least-authority dependency design

In dependency documentation, explicitly state:

> A consumer SHOULD receive only the minimum authority required to perform its responsibility.

Example:

Do not expose:

```text
DatabaseClient
```

when a capability needs only:

```text
saveProduct(...)
```

Do not expose:

```text
R2Client
```

when the capability needs only:

```text
uploadProductImage(...)
```

Ground this rule in actual repository contracts where possible.

---

# 38. Composition documentation must distinguish wiring from behavior

Create:

```text
docs/01-architecture/04-composition/
```

Document composition as:

```text
dependency selection
+
implementation wiring
+
runtime binding
```

not as a place for:

* business rules,
* domain validation,
* persistence semantics,
* duplicated workflows.

Clearly document actual composition roots and wiring modules.

For each important composition boundary document:

```text
COMPOSES:
PROVIDES:
REQUIRES:
MUST_NOT_OWN:
```

---

# 39. Runtime boundaries must be concrete

Under:

```text
06-runtime-boundaries/
```

document actual runtime distinctions such as:

* browser,
* server,
* Node,
* edge if used,
* Android/native,
* iOS/native,
* isolated services,
* local runtime.

For packages with multiple exports, document which export is valid in which runtime.

Example:

```text
@asol/example
RUNS_IN → browser-safe environments

@asol/example/server
RUNS_IN → server only
```

Use actual exports and package names.

---

# 40. Service boundaries must preserve capability ownership

Document that independently deployed services do not automatically become independent owners of shared capabilities.

For relevant services explain:

```text
Service:
Deployment boundary:
Composition boundary:
Owned runtime responsibility:
Shared capability dependencies:
Forbidden duplicated capability logic:
```

Use actual repository services.

---

# 41. Single Responsibility must apply to documentation too

Create:

```text
docs/01-architecture/01-principles/single-responsibility.md
```

Document the existing SRP rule for source files and packages.

Also apply SRP to documentation:

> One document should describe one coherent architectural subject and have one primary reason to change.

Do not create giant catch-all documents.

Do not over-fragment into tiny documents for individual functions.

---

# 42. Keep current architecture separate from history

Documents under:

```text
01-principles/
02-packages/
03-dependencies/
04-composition/
05-capability-enforcement/
06-runtime-boundaries/
07-enforcement/
08-reference/
```

MUST describe current architecture and current rules.

Historical migrations, consolidation work, previous audits, old package layouts, and architectural decisions belong under:

```text
09-decisions/
```

when they remain useful.

Do not mix historical narrative into canonical current-state documents.

---

# 43. Convert useful historical knowledge into ADRs

Review historical backup files such as:

* consolidation reports,
* capability consolidation follow-ups,
* SRP split reports,
* architecture enforcement migrations,
* UI capability package migrations,
* major package boundary changes.

When a historical document contains an architectural decision that explains the current structure, convert the relevant knowledge into an ADR.

Use:

```text
docs/01-architecture/09-decisions/ADR-xxxx-*.md
```

Each ADR should contain:

```text
# ADR-xxxx: Title

## Status
## Context
## Decision
## Alternatives Considered
## Consequences
## Affected Architecture
## Current Relevance
## Related Documents
```

Use statuses such as:

```text
Accepted
Superseded
Deprecated
Historical
```

as appropriate.

---

# 44. Historical ADRs are not current sources of truth

`09-decisions/README.md` MUST explicitly state:

> ADRs explain why decisions were made. Current architecture documents take precedence when an old ADR describes a superseded implementation.

Agents must not treat old ADR implementation details as current architecture.

---

# 45. Move domain-specific operational detail out when appropriate

If backup documentation contains detailed information that clearly belongs in another documentation area such as:

```text
docs/02-data-and-storage/
docs/03-products-and-commerce/
docs/04-ui-components/
docs/05-platform-features/
docs/06-super-admin-and-operations/
docs/07-mobile-and-release/
```

do not duplicate the full operational detail in architecture documentation.

Keep only the architectural relationship in `01-architecture/`.

Then link to the domain-specific canonical document.

However:

> Do not remove knowledge from `01-architecture-backup/` unless you verify that it will still exist somewhere appropriate.

If the information would otherwise disappear, move or preserve it appropriately.

---

# 46. Source Map is mandatory for important documents

Important capability, package, runtime, composition, and enforcement documents MUST contain:

```text
## Source Map
```

Use actual repository locations.

Example:

```text
Package:
`packages/...`

Public export:
`@asol/...`

Composition:
`src/features/...`

Implementation:
`...`

Architecture enforcement:
`scripts/...`

Tests:
`...`
```

The purpose is to let an agent jump directly from documentation to code.

---

# 47. Related Documents must use exact links

Each document SHOULD contain:

```text
## Related Documents
```

Use real relative Markdown links.

Do not say:

```text
See the package documentation.
```

Say:

```text
See [Package Exports](../02-packages/package-exports.md).
```

Validate links after reconstruction.

---

# 48. Change Impact is mandatory for important architecture documents

Important documents MUST contain:

```text
## Change Impact
```

Explain what must be reviewed when this architecture changes.

Examples:

* dependent packages,
* ports,
* composition,
* export maps,
* runtime exports,
* infrastructure ownership,
* architecture enforcement,
* related documentation.

This allows future agents to reason about blast radius.

---

# 49. Document failure behavior where architecture depends on it

When a capability or architecture boundary has meaningful failure semantics, include:

```text
## Failure Behavior
```

Describe:

* where failure originates,
* whether errors are translated,
* whether the capability retries,
* whether the caller may bypass on failure,
* what invariants remain mandatory.

Do not invent behavior not present in code.

---

# 50. Avoid prose-only architectural descriptions

Do not write long narrative paragraphs when structured relationships would be clearer.

Prefer:

```text
Owner:
Public Gateway:
Allowed Consumers:
Forbidden Consumers:
Composition:
Final Side Effect:
```

over vague architectural essays.

Use prose primarily to explain rationale and non-obvious constraints.

---

# 51. Optimize for semantic retrieval

Assume an agent may receive only a small retrieved chunk of a document.

Therefore:

* use meaningful headings,
* repeat the exact subject identifier in important sections,
* avoid relying on “as explained above,”
* avoid pronouns with unclear referents,
* make important rules understandable locally,
* keep each section semantically self-contained.

Do not require the agent to read an entire 500-line file to interpret one rule.

---

# 52. Avoid context-dependent references

Avoid:

```text
this
that
the above layer
the package mentioned earlier
the service described previously
```

when a specific identifier can be repeated.

Agent retrieval often returns isolated sections.

Each important rule must remain understandable in isolation.

---

# 53. Use explicit negative knowledge

Agents need to know what not to do.

In addition to `Forbidden Bypasses`, document relevant:

```text
MUST NOT IMPORT
MUST NOT OWN
MUST NOT COMPOSE
MUST NOT CALL
MUST NOT EXPOSE
MUST NOT INSTANTIATE
```

rules.

Negative knowledge is a first-class part of architecture documentation.

---

# 54. Keep terminology globally consistent

Create and consistently use a small architectural vocabulary.

Examples:

```text
Capability
Owner Package
Public Gateway
Port
Contract
Adapter
Composition Root
Infrastructure Owner
Final Side Effect
Mandatory Gateway
Sealed Package
Capability Closure
Runtime Boundary
```

Do not use multiple words for the same architectural concept unless explicitly defining the distinction.

If useful, add a glossary document.

You MAY create:

```text
docs/01-architecture/00-glossary.md
```

or an equivalent location if the vocabulary is large enough to justify it.

---

# 55. Verify package names and exports mechanically where possible

Do not manually guess the package catalog.

Inspect actual:

```text
packages/*/package.json
```

and their `exports`.

Ensure documentation exactly matches:

* package names,
* entry points,
* runtime subpaths,
* dependencies.

Detect stale package names from the backup.

---

# 56. Verify capability ownership from behavior, not filenames

A package name is not sufficient evidence that it owns a capability.

Trace actual execution and side effects.

For each significant capability determine:

```text
Who validates it?
Who performs authoritative domain logic?
Who exposes the public gateway?
Who reaches infrastructure?
Who owns the final side effect?
```

Use those facts to document ownership.

---

# 57. Verify end-to-end execution paths

For important capabilities, trace actual execution from caller to side effect.

Inspect all relevant:

* pages,
* hooks,
* routes,
* server actions,
* services,
* composition,
* capability packages,
* contracts,
* adapters,
* infrastructure.

Do not document theoretical flows.

Document current flows.

---

# 58. Preserve architectural intent that code alone cannot express

The backup may contain valuable rationale such as:

* why direct imports are forbidden,
* why composition exists,
* why package ownership was chosen,
* why a capability was consolidated,
* why a runtime boundary exists.

If the rationale is still consistent with current architecture, preserve it even if it does not appear explicitly in code.

Do not reduce documentation to an API inventory.

---

# 59. Do not preserve obsolete implementation claims as current truth

If deep verification proves an old statement is no longer true:

Do NOT reproduce it in current architecture documents.

If historically important, preserve it as an ADR with appropriate status.

Example:

```text
Status: Superseded
```

---

# 60. Be extremely conservative before declaring old information wrong

A backup statement may only be omitted as obsolete after deep verification.

Before excluding a significant architectural claim, inspect enough repository evidence to answer:

```text
Old claim:
Current implementation:
Did only the mechanism change?
Did ownership change?
Did the invariant change?
What replaced the old behavior?
```

Only then decide.

Do not use “not immediately found” as evidence.

---

# 61. Enforcement exceptions must be explicit

Create:

```text
docs/01-architecture/07-enforcement/enforcement-exceptions.md
```

For every real exception document:

```text
Exception:
Scope:
Reason:
Why it does not violate capability ownership:
Risk:
Removal Condition:
Source:
```

Avoid undocumented exceptions.

Avoid broad exception categories.

---

# 62. Do not legitimize bad exceptions merely because they exist

If current code contains an enforcement exception that appears to violate the documented architecture:

Investigate it.

Do not automatically document it as intended architecture.

Determine whether it is:

```text
valid exception
legacy compatibility
temporary workaround
architectural debt
obsolete rule
```

If this task is documentation-only for that case, document its real status accurately rather than silently normalizing it.

---

# 63. README.md must be the navigation root for agents

Create:

```text
docs/01-architecture/README.md
```

as the primary architecture entry point.

It MUST explain:

* what this documentation covers,
* how it is organized,
* recommended reading order,
* which documents are canonical,
* where capability ownership is defined,
* where package rules are defined,
* where dependency rules are defined,
* where composition rules are defined,
* where enforcement rules are defined,
* where runtime boundaries are defined,
* where historical ADRs are stored,
* how an agent should determine what to read before modifying a capability.

It SHOULD include a task-oriented navigation section such as:

```text
If changing a package:
→ read ...

If creating a package:
→ read ...

If adding infrastructure access:
→ read ...

If changing composition:
→ read ...

If adding a service:
→ read ...

If changing runtime exposure:
→ read ...

If architecture:check fails:
→ read ...
```

Do not turn README into a duplicate of all documentation.

---

# 64. Current-state reference documents must be canonical

At minimum treat these as primary reference sources:

```text
08-reference/capability-map.md
08-reference/package-catalog.md
08-reference/dependency-map.md
```

Other documents SHOULD link to these where appropriate rather than recreating competing maps.

---

# 65. Validate all internal documentation links

After reconstruction:

Check all links inside `docs/01-architecture/`.

Ensure:

* no links point to deleted old paths,
* cross-document links resolve,
* links to documentation outside architecture still resolve,
* ADR links resolve,
* source paths are current.

---

# 66. Search the rest of docs for references to deleted architecture paths

Because the old `docs/01-architecture/` layout will be removed, search all of:

```text
docs/
```

for references to old architecture document paths.

Update references where necessary so documentation links do not break.

Do not rewrite unrelated documentation unnecessarily.

Only update links or references affected by this architecture reconstruction.

---

# 67. Search repository instructions for references to old architecture paths

Search relevant root instructions and agent files for hardcoded references to documents that moved.

Examples may include:

```text
AGENTS.md
CLAUDE.md
README files
package documentation
```

If a binding instruction points to a moved architecture document, update the path while preserving the rule.

Do not alter unrelated agent rules.

---

# 68. Validate documentation against repository behavior after writing

After all new documents are written, perform a second verification pass:

```text
New architecture documentation
↕
Current repository
```

Re-check:

* package catalog,
* package exports,
* capability ownership,
* dependency relationships,
* composition roots,
* runtime boundaries,
* infrastructure ownership,
* mandatory gateways,
* architecture enforcement.

Correct every discrepancy found.

---

# 69. Perform internal consistency validation

Search the new documentation for contradictions.

Detect:

* multiple owners for one capability,
* incompatible package layers,
* conflicting dependency permissions,
* inconsistent runtime statements,
* inconsistent public exports,
* duplicated sources of truth,
* conflicting definitions,
* stale paths,
* inconsistent terminology.

Resolve all contradictions.

---

# 70. Perform backup coverage verification

After reconstruction, review every file under:

```text
docs/01-architecture-backup/
```

again.

Ensure its meaningful knowledge is accounted for.

For each backup document determine where its information ended up.

Do not leave this as an assumption.

---

# 71. Do not create empty skeleton documents

Every required file must contain meaningful verified content.

Do not create placeholder files just to match the desired directory tree.

If, after deep analysis, one proposed document genuinely has no independent responsibility, you MAY redesign that portion of the structure.

However, preserve the architectural intent of the requested taxonomy.

Explain any significant deviation in the final report.

---

# 72. Additional directories are allowed

You MAY create additional sections if they materially improve the knowledge architecture.

Possible examples, only if justified:

```text
10-security-boundaries/
11-data-architecture/
12-platform-boundaries/
```

Do not create them unless they represent genuine distinct architectural subjects.

---

# 73. Keep documentation comprehensive, not artificially short

Do not optimize for few lines.

Preserve all important architecture knowledge.

However:

> Comprehensive does not mean repetitive.

Prefer one detailed canonical explanation plus links over five duplicated explanations.

---

# 74. Do not use visual polish as a goal

This documentation is for agents.

Do not spend effort on:

* decorative graphics,
* screenshots,
* presentation styling,
* visually elaborate Mermaid diagrams,
* human-oriented storytelling.

Optimize effort for:

* correctness,
* explicit relationships,
* exact identifiers,
* invariants,
* searchability,
* traceability,
* local semantic completeness.

---

# 75. Agent comprehension test

Before considering the work complete, verify that a new coding agent could answer the following using `docs/01-architecture/`:

```text
What are the repository's architecture principles?

What is a capability?

Who owns each significant capability?

What is the public gateway for each capability?

Which package owns each capability?

What architectural layer does each package belong to?

Which packages may depend on which other packages?

Which dependencies are forbidden?

How do ports and contracts work?

Where are contracts implemented?

Where does dependency wiring happen?

What are the composition roots?

What is the expected dependency direction?

What are the runtime boundaries?

Which exports are browser-safe?

Which exports are server-only?

Who owns direct database access?

Who owns storage access?

Who owns native integrations?

Which vendor SDKs are restricted?

What is a mandatory gateway?

What makes a capability CLOSED?

What bypasses are explicitly forbidden?

How are packages sealed?

How does architecture:check enforce boundaries?

How are dynamic imports handled?

How are deep imports handled?

How are relative package imports handled?

How are new packages treated?

How are unknown new source directories treated?

What exceptions exist?

How should a new package be created?

How should a new capability be introduced?

What must be reviewed when changing an existing capability?

Where is the canonical capability map?

Where is the canonical package catalog?

Where is the canonical dependency map?

Why were major architecture decisions made?
```

If the documentation cannot answer these unambiguously, it is incomplete.

---

# 76. Machine-retrieval test

Assume an agent receives only one retrieved subsection.

Ensure that subsection is still understandable.

Important sections must not depend heavily on phrases such as:

```text
as described earlier
as shown above
this package
that service
the previous layer
```

Repeat exact identifiers where necessary.

Optimize for chunk-level semantic retrieval.

---

# 77. Change-safety test

For each major architecture document ask:

> Could an agent follow this documentation and accidentally bypass the intended architecture?

If yes, the documentation is too vague.

Add:

* exact allowed path,
* exact forbidden path,
* ownership,
* rationale,
* source locations.

---

# 78. Architecture extension test

Verify that documentation clearly explains what an agent should do when introducing:

* a new capability,
* a new package,
* a new port,
* a new adapter,
* a new infrastructure SDK,
* a new runtime entry point,
* a new service,
* a new composition boundary.

The documentation should guide extension without requiring architectural guesswork.

---

# 79. Final correctness hierarchy

When resolving conflicts, use this hierarchy carefully:

```text
Explicit current repository invariants and enforced architecture
↓
Current code behavior
↓
Current package exports and composition
↓
Verified architectural intent
↓
Backup architecture documentation
↓
Historical implementation details
```

Do not use this hierarchy mechanically.

A current implementation bug must not automatically redefine architecture.

Use enforcement, repeated structural patterns, tests, current documentation, and architectural intent together to distinguish:

```text
current architecture
```

from:

```text
current accidental implementation
```

---

# 80. Final required outcome

The result must NOT be:

```text
The old documentation reorganized into folders.
```

The result must be:

```text
All valid architectural knowledge from the backup
+
deep verification against the current repository
+
current package and capability ownership
+
current dependency relationships
+
current composition model
+
current runtime boundaries
+
current infrastructure ownership
+
mandatory gateway semantics
+
default-deny behavior
+
actual architecture enforcement
+
historical architectural rationale
+
Agent-First structured documentation
=
a canonical architecture knowledge base for coding agents
```

`docs/01-architecture/` must become the authoritative, current, structured architecture knowledge base.

`docs/01-architecture-backup/` must remain untouched as the preserved original source.

---

# 81. Do not claim completion prematurely

Do not claim the task is complete merely because the requested files exist.

Completion requires:

1. Every backup file was read.
2. Every meaningful backup architectural claim was considered.
3. Current code was inspected deeply.
4. Package ownership was verified.
5. Capability ownership was verified.
6. Dependency relationships were verified.
7. Composition was verified.
8. Runtime boundaries were verified.
9. Infrastructure ownership was verified.
10. Enforcement behavior was verified.
11. Canonical reference maps were built.
12. Historical decisions were separated from current truth.
13. New documentation was internally cross-checked.
14. New documentation was checked against the current repository again.
15. Backup knowledge coverage was verified.
16. Broken documentation references caused by the migration were corrected.
17. No meaningful valid architectural knowledge was silently lost.

If any meaningful area cannot be fully verified, state it explicitly rather than guessing.

---

# 82. Important constraints

Do not modify:

```text
docs/01-architecture-backup/
```

Do not delete valid project knowledge.

Do not fabricate architecture.

Do not document intended behavior as current behavior unless verified.

Do not preserve obsolete behavior as current truth.

Do not use images as architectural sources of truth.

Do not create duplicate canonical sources.

Do not create placeholder documentation.

Do not convert the documentation into generic software architecture theory.

Document **this repository specifically**.

---

# Final report

After completing the work, provide a concise final report containing only:

* the final directory structure,
* number of new architecture documents,
* any additional directories created and why,
* the main canonical sources of truth created,
* major duplicate knowledge consolidated,
* major historical information converted into ADRs,
* any significant backup claims intentionally omitted because deep verification proved them obsolete,
* any documentation moved to another `docs/` domain,
* confirmation that `docs/01-architecture-backup/` was not modified,
* any meaningful area that could not be fully verified.

Do not provide a long file-by-file narrative.

The priority is:

> **Perform the complete reconstruction first, verify it deeply, and report the result briefly.**
